void insertionSort(int arr[], int n)
{
    int i,j,temp;
    int swapd=0;
    for(i=0;i<n;i++)
    {
        j=i;
        while(j>0 && arr[j-1]>arr[j])
        {
            temp=arr[j];
            arr[j]=arr[j-1];
            arr[j-1]=temp;
            j--;
        }
    }
}
